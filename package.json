{
  "name": "omp-soft-boundary-guard",
  "version": "1.8.3",
  "description": "Opt-in OMP soft boundary guard with deterministic adapters and advisory confirmation for uncertain writes.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "release:check": "bun run scripts/release-check.ts && bun node_modules/typescript/bin/tsc --noEmit && bun test",
    "handoff": "bun run bin/repository-mutation-handoff.ts",
    "reinstall": "bun run scripts/reinstall.ts"
  },
  "keywords": ["omp-plugin", "soft-boundary", "boundary-guard", "safety"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/klondikemarlen/omp-soft-boundary-guard.git"
  },
  "omp": {
    "extensions": ["./index.ts"]
  },
  "dependencies": {
    "shell-quote": "^1.10.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
